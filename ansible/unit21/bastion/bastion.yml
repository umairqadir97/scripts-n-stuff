---
# Usage: ansible-playbook <path/filename> -k
# Runs against IPs/Hostnames in hosts file
#
- hosts: all
  gather_facts: False
  remote_user: ubuntu
  become: yes
  become_user: root
  become_method: sudo

  tasks:
    - name: Perform Updates
      raw: apt update && apt upgrade -y

    - name: Install OpenJDK
      package: name=openjdk state=latest

    - name: Install kafka

